export const SCORE_FILE_TYPES = [".mxl", ".musicxml", ".xml", ".mxmls", ".pdf", ".png", ".jpg", ".jpeg"]
export const SCORE_FILE_TYPES_TEXT = SCORE_FILE_TYPES.map(s => s.substring(1).toUpperCase()).join(", ")
export const AUDIO_FILE_TYPES = [".mp4", ".mp3", ".mov", ".wav", ".ogg", ".avi", ".m4a"]
export const AUDIO_FILE_TYPES_TEXT = AUDIO_FILE_TYPES.map(s => s.substring(1).toUpperCase()).join(", ")
